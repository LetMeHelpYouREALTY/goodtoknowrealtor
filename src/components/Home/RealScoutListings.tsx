'use client';

import { RealScoutOfficeWidget } from '@/components/RealScout/RealScoutOfficeWidget';

/** @deprecated Prefer RealScoutOfficeWidget directly; kept for legacy imports. */
export function RealScoutListings() {
  return <RealScoutOfficeWidget />;
}
